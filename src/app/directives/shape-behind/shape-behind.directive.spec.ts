import { ShapeBehindDirective } from './shape-behind.directive';

describe('ShapeBehindDirective', () => {
  it('should create an instance', () => {
    const directive = new ShapeBehindDirective();
    expect(directive).toBeTruthy();
  });
});
