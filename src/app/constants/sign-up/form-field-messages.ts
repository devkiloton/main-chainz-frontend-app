export const formFieldMessages = {
  requiredPassword: $localize`You must enter a password` as string,
  requiredEmail: $localize`You must enter an email` as string,
  requiredName: $localize`You must enter a name` as string,
  invalidEmail: $localize`Not a valid email` as string,
  invalidPassword:
    $localize`Password must contain at least 8 characters, 1 uppercase, 1 lowercase, 1 number and 1 special character` as string,
  invalideName: $localize`Name must contain at least 2 characters` as string,
};
