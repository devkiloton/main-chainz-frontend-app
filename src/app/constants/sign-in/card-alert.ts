export const cardAlert = {
  requiredEmail: $localize`Type a valid email` as string,
  requiredPassword: $localize`Type a valid password` as string,
  invalidData: $localize`Invalid data, try again` as string,
  problems: $localize`Something went wrong, try again later` as string,
  generalError: $localize`Fill up all fields` as string,
};
